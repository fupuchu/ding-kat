<div class="container">
    <div class="row">
    <div class="col-lg-12 text-center">

      <h1>MENU OPTIONS</h1>
      <h2>You are currently on <%= @current %>
        <div class="container">
            <div class="row">
            <% @menus.each do |menu| %>
                <div class="card" style="width: 18rem;">
                    <img class="card-img-top" <%= image_tag menu.img_src %>
                    <div class="card-body">
                        <h5 class="card-title"><%= menu.name %></h5>
                        <h5 class="card-title">$<%= menu.price %>/week</h5>
                    <div class="card-text">
                        <p><%= menu.line_1 %></p>
                        <p><%= menu.line_2 %></p>
                        <p><%= menu.line_3 %></p>
                        <p><%= menu.line_4 %></p>
                    </div>
                    <% if @current == menu.id %>
                        <%= link_to  "Unsubscribe", unsubscribe_path, method: :put, data: {confirm: 'Are you sure you want to unsubscribe?'} , class: "btn btn-warning" %>
                    <% else %>
                        <%= link_to  "Select This", select_menu_path(menu), method: :put , class: "btn btn-warning" %>
                    <% end %>
                    </div>
                </div>
            <% end %>

        </div>
        </div>

        <div class="row">

          <div class="col-sm-3">
          </div>

          <div class="col-sm-3">
          </div>

        </div>
        <br>
        <%= link_to  "Balik Kampong (Back to Home)", root_path , class: "btn btn-secondary" %>
    </div>
    </div>
</div>
