<div class="container">
    <div class="row">
    <div class="col-lg-12 text-center">

      <h1>SUBSCRIPTION OPTIONS</h1>
      
      <button><%= link_to  "Back to Home", root_path %></button> 
      <br>
      <br>
      <div class='row-fluid'>
      <% @subs.each do |sub| %>
      <div class="span6 text-left"><%= sub.name %> $<%= sub.price %></div> <div class="span6 text-left"><button class='subsc-button'><%= link_to  "Select", subscription_path(sub) %></button></div> <br>
      <% end %>
      </div>

    </div>
    </div>
</div>
